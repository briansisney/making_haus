<%= simple_form_for(@grocery_item) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :ingredient %>
    <%= f.input :amount %>
    <%= f.association :grocery_unit %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
