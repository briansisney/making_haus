<%= simple_form_for(@meal_dish) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :meal %>
    <%= f.association :dish %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
