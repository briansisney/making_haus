<%= simple_form_for(@expense_responsibility) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :expense %>
    <%= f.association :user %>
    <%= f.input :amount %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
