<%= simple_form_for(@meal) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :meal_date %>
    <%= f.association :week %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
