<%= simple_form_for(@guest_list) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.input :guest_id %>
    <%= f.association :week %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
